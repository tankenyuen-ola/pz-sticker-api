version: "3.8"

services:
  pz-sticker-api:
    build: .
    container_name: pz-sticker-api
    network_mode: host
    env_file:
      - .env
    volumes:
      - ./work_dir:/app/work_dir
    restart: unless-stopped
